# let d a = a;;
val d : 'a -> 'a = <fun>
(* 'a indica um generic. Em ocaml, essas letras que representam qualquer coisa que recebederao um tipo, sao proununciados
   conforme no alfabeto grego, ou seja, 'a -> alpha, 'b -> beta, etc.

   Esses generics podem ter nomes mais descritivos, mas pro padrao ocaml retorna 'a, 'b, etc.
 *)